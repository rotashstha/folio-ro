# Anthropic API key — used by /api/brain to call Claude Haiku directly.
# Get yours at console.anthropic.com
ANTHROPIC_API_KEY=

# (Optional) If you deploy the Cloudflare Worker instead, set this and
# swap the /api/brain route to proxy here rather than calling Claude directly.
WORKER_URL=
